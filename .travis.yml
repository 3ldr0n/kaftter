matrix:
  include:
    - language: java
      jdk:
        - openjdk11
      before_script:
        - cd kaftter-api
      script:
        - ./gradlew clean
        - ./gradlew build -x test
        - ./gradlew test
    - language: java
      jdk:
        - openjdk11
      before_script:
        - cd kaftter-consumer
      script:
        - ./gradlew clean
        - ./gradlew build -x test
        - ./gradlew test
    - language: scala
      jdk:
        - oraclejdk8
      scala:
        - 2.12.10
      before_script:
        - cd tweet-summarizer
      script:
        - sbt clean
        - sbt package
        - sbt test
